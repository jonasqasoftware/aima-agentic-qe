{
  "name": "aima-agentic-qe",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Laboratório determinístico de estratégia de qualidade orientada por riscos e frameworks AIMA.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "analyze": "node src/cli.js analyze-pr",
    "serve": "node src/cli.js serve",
    "evaluate": "node src/cli.js evaluate --change examples/payment-refactor.change.json --expected evals/golden/payment-refactor.expected.json",
    "test": "node --test",
    "check": "node --check src/*.js && npm test && npm run evaluate"
  },
  "license": "MIT"
}
